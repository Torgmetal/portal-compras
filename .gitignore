node_modules/
.next/
.vercel/
*.tsbuildinfo

.DS_Store

/lib/generated/prisma

# Variáveis de ambiente — NUNCA commitar (contém senhas)
.env
.env.local
.env.*.local
.env.development
.env.production
.vercel

# Configuração local do Claude Code Desktop (não versionada — cada dev tem a sua)
.claude/settings.local.json

# Duplicatas que o iCloud Drive cria por conflito de sincronização
* 2.*
* 2
*\ 2.*
.env*.local
