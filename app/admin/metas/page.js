import MetasClient from "./MetasClient";

export const metadata = {
  title: "Metas e Objetivos — Workspace Torg",
  description: "Defina metas mensais e semanais por módulo e setor.",
};

export default function MetasPage() {
  return <MetasClient />;
}
