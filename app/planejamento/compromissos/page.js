import CompromissosClient from "./CompromissosClient";

export const metadata = {
  title: "Meus Compromissos — Torg Metal",
};

export default function CompromissosPage() {
  return <CompromissosClient />;
}
