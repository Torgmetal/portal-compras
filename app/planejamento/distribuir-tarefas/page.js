import DistribuirTarefasClient from "./DistribuirTarefasClient";

export const metadata = { title: "Distribuir tarefas (IA) — Planejamento Torg" };

export default function DistribuirTarefasPage() {
  return <DistribuirTarefasClient />;
}
